export type PageMeta = {
  author?: { name: string; picture?: string };
  category?: string;
  date?: string;
  description?: string;
  poster?: string;
  title: string;
  readTime: number;
  url: string;
};
